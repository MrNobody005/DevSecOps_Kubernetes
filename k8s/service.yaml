apiVersion: v1
kind: Service
metadata:
  name: devsecops-web
  namespace: devsecops
spec:
  type: NodePort
  selector:
    app: devsecops-web
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
